# Optional MapTiler API key — used to swap the default keyless OSM raster style
# for higher-quality vector tiles. Leave blank to use the bundled OSM raster style.
MAPTILER_KEY=

# Default sim configuration overrides (optional).
SIM_TICK_HZ=10
SIM_HOME_LAT=37.4275
SIM_HOME_LON=-122.1697
